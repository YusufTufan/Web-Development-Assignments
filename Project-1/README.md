# 💻 Project 1: Single-Page React Portfolio

## 📝 Overview

This project is a static **single-page portfolio** application built using **React**. The primary focus was on implementing ready-made UI components from a modern library and establishing a basic React functional component structure. The portfolio is populated with randomized content (not personal information) to fulfill the requirements, including sections for educational status, past experiences, skills, projects, and a photograph.

## ✨ Key Features

* **Single-Page Layout (SPA):** All sections are presented on one page.
* **Component Library:** Utilizes **Flowbite React** components for a structured and clean UI.
* **Content:** Includes sections for Personal Information, Education, Experience, Skills, and Projects.

## 🛠️ Technologies Used

* **Frontend:** React (JavaScript/JSX)
* **UI:** Flowbite React Component Library

## ☁️ Setup Instructions

1.  Navigate to the project directory: `cd Project-1`
2.  Install dependencies: `npm install` or `yarn install`
3.  Run the application: `npm start` or `yarn start`
